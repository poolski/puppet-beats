  http:
    ports: [<% @http_ports.each do |port| -%><%= port %>, <% end -%>]
    hide_keywords: [<% @hide_keywords.each do |kw| -%>"<%= kw %>",<% end -%>]
    send_headers: [<% @send_headers.each do |header| -%>"<%= header %>",<% end -%>]
    real_ip_header: "<%= @real_ip_header %>"
    split_cookie: <%= @split_cookie %>
    
